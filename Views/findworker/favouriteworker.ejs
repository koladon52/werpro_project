<%- include("../partials/header")%>
<head>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
  <script src="http://ajac.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
  <script src="jQueryA.js"></script>  
  <script src="jquery-ui.js"></script>
  <Link href="jquery-ui.css" rel="stylesheet">
  <Link href="http://code.jquery.com/ui/1.10.4/themes/ui-lightness/jquery-ui.css" rel="stylesheet" >
  <link rel="stylesheet" href="/css/list.css">
</head>

<div class="container col-xs-12 col-sm-12 col-md-11 col-lg-8">
  <div class="list col-lg-12 col-md-12 col-sm-12 align-self-center">
  <h1 class="m-4">
    </h1>
    <% if(favouriteworker.favouriteresume.length == "0"){ %>
      <h1><center>ไม่พบงานที่ตรงกัน</center></h1>
      <% } else { %>
        <h1><center>พบงานที่ตรงกัน <%= favouriteworker.favouriteresume.length %></center></h1>
      <%}%>
<% favouriteworker.favouriteresume.forEach(function(resume){ %>
<!-- <div class="row">
    <div class="col-md-7">
      <a href="#">
        <img class="img-fluid rounded mb-3 mb-md-0" src="/resumeimage/<%= resume.image %>" width="300" height="300"  alt="">
      </a>
    </div>
    <div class="col-md-5">
          <h4>ชื่อ-สกุล           :  <%= resume.firstname %> <%= resume.lastname%> </h4>
          <h5>ประเภทการจ้างงาน   : <%= resume.employmenttype %></h5>
          <h5>ประเภทงาน        : <%= resume.jobtype %> </h5>
          <h5>ช่วงเวลาที่สะดวกทำงาน : <%= resume.starttime%> - <%= resume.finishtime %> น.</h5>
          <h5>วันทีี่แก้ไขล่าสุด      : <%= resume.editdate%></h5>
      <a action="GET" class="btn btn-primary" href="/findworker/workerlist/<%= resume._id %>">see more detail</a>
    </div>
  </div> -->
  <div class="card border-light m-2">
          <div class="row m-1">
            <div class="col-lg-5 col-md-5 col-sm-5 col-xs-12">
              <a href="#">
                <img style="width: 700px; height: 250px; object-fit: cover;" class="img-fluid rounded" src="/resumeimage/<%= resume.image %>" alt="">
                <!-- <img style="width: 600px; height: 250px; object-fit: cover;" class="img-fluid rounded mb-3 mb-md-0"  src="/resumeimage/<%= resume.image %>" alt=""> -->
              </a>
            </div>
            <div class="col-lg-7 col-md-7 col-sm-7 col-xs-12">
              <div class="row col-lg-12 font-weight-bold h4 mt-4">
                <p> 
              </div>
              <div class="row col-lg-12 h4">
                <p id="detail" >ชื่อ-สกุล <%= resume.firstname %> <%= resume.lastname%></p>
              </div>
              <div class="row col-lg-12 justify-content-between">
                <div class="row col-lg-6">
                  <p><i class="far fa-handshake handshake"></i> <%= resume.employmenttype %></p>
                </div>
              <div class="row col-lg-6">
                  <p><i class="fas fa-tasks tasks"></i> <%= resume.jobtype %></p>
              </div>
              </div>
              
              <div class="row col-lg-12">
                <p><i class="far fa-money-bill-alt money"></i> <%= resume.salary %></p>
              </div>
              <div class="row col-lg-12">
                <p><i class="fas fa-clock clock"> </i> ช่วงเวลาที่สะดวกทำงาน : <%= resume.starttime%> - <%= resume.finishtime %> น.</p>
              </div>
              <div class="row col-lg-12">
                <h6><i  class="fas fa-hourglass-half "></i> โพสต์เมื่อ <%= resume.postdate %> น. </h6>
              </div>
              <div class="col-lg-12 mb-3" align="right">
                <a action="GET" class="btn btn-primary btn-readmore" href="/findworker/workerlist/<%= resume._id %>">อ่านเพิ่มเติม</a>
              </div>
            </div>
          </div>
  </div>

  <% })  %>
</div>
</div>

<%- include("../partials/footer")%>

